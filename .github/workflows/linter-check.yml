name: linter-check

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -name: Install dependencies and check
       run: |
          pip install poetry
          make install
          make lint
