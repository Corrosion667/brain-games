name: linter-check

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies and do linter check
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          make install
          make init


